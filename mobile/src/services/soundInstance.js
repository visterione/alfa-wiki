import {createSound} from 'react-native-nitro-sound';

/**
 * Один экземпляр нативного звукового движка на всё приложение.
 *
 * Библиотека экспортирует по умолчанию legacy-прокси, но её собственные хуки
 * работают через createSound() — им же пользуемся и мы. Экземпляр обязан быть
 * общим для записи и воспроизведения: слушатели прогресса хранятся в полях
 * нативного объекта, и подписка на одном экземпляре не увидит событий другого.
 */
const sound = createSound();

export default sound;
