-- Общие исполнители шагов живут в заготовке должности как значения по
-- умолчанию. При создании вакансии они копируются в сетевые назначения;
-- филиал может заменить их локальными назначениями без изменения шаблона.
ALTER TABLE vac_templates
  ADD COLUMN IF NOT EXISTS assignments JSONB NOT NULL DEFAULT '{}'::jsonb;
