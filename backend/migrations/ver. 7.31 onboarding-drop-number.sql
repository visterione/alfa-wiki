-- ver. 7.31 — убираем номер заявки онбординга.
--
-- Номер заводился как «человеческий идентификатор» вроде кода задачи, но в
-- процессе он ни разу не понадобился: заявку всегда называют по врачу, а не
-- «№17». В интерфейсе, письмах и уведомлениях его больше нет, а колонка с
-- последовательностью осталась бы тем самым мёртвым весом, из-за которого
-- следующий человек полчаса выясняет, кто её читает.
--
-- Миграция идемпотентна: колонки и последовательности может уже не быть.

ALTER TABLE onb_applications DROP COLUMN IF EXISTS number;
DROP SEQUENCE IF EXISTS onb_application_number_seq;
