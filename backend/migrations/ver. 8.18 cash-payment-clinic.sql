-- Выдача из кассы до этой версии знала только врача и период, но не медцентр.
-- В сводке из-за этого одна выдача гасила общий остаток врача и печаталась
-- одинаковой суммой в строке каждого медцентра. Медцентр теперь указывается
-- явно при выдаче.

ALTER TABLE cash_payments
  ADD COLUMN IF NOT EXISTS "clinicId" VARCHAR(50);

-- Старые выдачи намеренно остаются без медцентра: угадывать его нельзя, а
-- разнести руками нужно там же, где они видны — в разделе «Архив → Касса».
-- Пока медцентр не проставлен, сводка показывает такую выдачу отдельной
-- строкой «без медцентра» и в остаток клиники её не включает.
